/* class Pessoa {
    nome;
    idade;

    constructor(nome, idade) {
        this.nome = nome;
        this.idade = idade;
    }

    descrever() {
        console.log(`Nome: ${this.nome}; Idade: ${this.idade};`);
    }
}

new Pessoa('Renan', 30).descrever() */;

/* 2 - O que será impresso?

class Pessoa {
    nome;
    idade;

    constructor(nome, idade) {
        this.nome = nome;
        this.idade = idade;
    }
}

function descreverPessoa(pessoa) {
    console.log(`Nome: ${pessoa.nome}; Idade: ${pessoa.idade};`);
}

const renan = new Pessoa('Renan', 30);
console.log(descreverPessoa(renan)); */

// 3 - Tendo em vista os objetos literais qual o resultado esperado para da execução em um terminal do código a seguir?

 /*    const dev = {
        nome: 'Renan',
        linguagemPreferida: 'JavaScript'
    };

    console.log(dev.linguagemPreferida); */

// 4 - Tendo em vista o código abaixo qual a maneira mais simples para a criação de uma instância de pessoa?

/* class Pessoa {
    nome;
    idade;

    constructor(nome, idade) {
        this.nome = nome;
        this.idade = idade;
    }
} */

//new Pessoa('Renan'.30)
