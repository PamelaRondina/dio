/* 1) Importar (usando 'require') e exportar (com 'module.exports') módulos são oferecidos por qual tecnologia?

Node.js (com JavaScript)
*/

/* 2) Qual das opções utiliza o conceito de destructuring na importação de um módulo com várias funções auxiliares, inclusive a 'gets'? 

const {gets} = requires('./nomeArquivo')
*/

/* 03) Assinale a sintaxe correta para importação de um módulo: 

const funcoes = require('./nomeArquivo')
*/

/* 04) Qual das alternativas abaixo exporta as funções 'gets' e 'print' em um objeto literal?

module.exports = { gets, print };
*/

